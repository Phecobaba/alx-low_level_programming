#include <stdio.h>
/**
 * main - Entry point
 *
 * Description: 'the program's description'
 *
 * Return: Always 0 (Success)
 */
int main(void)
{

	for (i = ‘a’; i <= ‘z’; i++)

	putchar(ch);

	putchar ("\n");
	return (0);
}
