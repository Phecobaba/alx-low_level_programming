#include <stdio.h>
/**
 * main - Entry point
 *
 * Description: 'the program's description'
 *
 * Return: Always 0 (Success)
 */
int main(void)
{
	int u;
	char bar;

	for (u = '0'; u <= '9'; u++)

	putchar(u);

	for (bar = 'a'; bar <= 'f'; bar++)

	putchar(bar);

	putchar('\n');

	return (0);
}
